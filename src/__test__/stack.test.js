'use strict';

const Stack = require('../lib/stack');

describe('stack.js', () => {
  test('#constructor', () => {

  });

  test('#pop should kill top node of stack', () => {

  });

  test('#peak should look just at node on top', () => {

  });

  test('#push should insert value at head of linkedlist', () => {

  });
});
