'use strict';

const Queue = require('../lib/queue');

let qTest;

beforeAll(() => {
  qTest = new Queue();
});

describe('#enQueue TESTS', () => {
  test('#constructor', () => {
    const qTest = new Queue();
  });
  test('#enqueue is actually head ', () => {
    const qTest = new Queue();
  });
  test('#engueue gets 1 more added', () => {
    const qTest = new Queue();
  });
});

describe('#deQueue TESTS', () => {
  test('#constructor', () => {
    const qTest = new Queue();
  });
  test('#dequeue loses 1 in list', () => {
    const qTest = new Queue();
  });
  test('#dequeue is null', () => {
    const qTest = new Queue();
  });
});
